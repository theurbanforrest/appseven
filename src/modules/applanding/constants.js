// @flow

//namespace the actions to prevent this module's action type from colliding
	export const NAME = 'applanding'


//the actions
	export const SHOW_FAUX_LOADING = `${NAME}/SHOW_FAUX_LOADING`;
	export const HIDE_FAUX_LOADING = `${NAME}/HIDE_FAUX_LOADING`;